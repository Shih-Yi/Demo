<%= form_for setup_event(@event), :url => event_path(@event), :method => :patch , :class => "form" do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%# 會自動找＿開頭的html.erb檔案  %>


  <%= f.submit "Update", :class => "btn btn-primary" %>
<% end %>
