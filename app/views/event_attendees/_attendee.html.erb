<li id="attendee-<%= a.id %>">

  <%= a.name%>
  <%= link_to "Show", event_attendee_path(@event, a), :class=> "btn"%>
  <%= link_to "Edit", edit_event_attendee_path(@event, a), :class=> "btn"%>
  <%= link_to "Delete", event_attendee_path(@event, a),:method => :delete, :remote => true, :class=> "btn"%>

</li>

