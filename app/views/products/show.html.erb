<div id="cart">
  <%= render "cart" %>
</div>
<h2><%= @product.name %></h2>

<%= form_tag buy_product_path(@product), :remote => true do %>
  <%= select_tag "qty", options_for_select( 1..10 ) %>
  <%= submit_tag "Buy", :class => "btn btn-danger btn-xs" %>
<% end %>

<%= image_tag(Faker::Avatar.image, :width => 180) %>

<%= simple_format @product.description %>
